# Variáveis de ambiente (API keys, tokens — NUNCA commitar)
.env
.env.local
.env.*

# Arquivo gerado pelo /setup com MCPs específicos do usuário
_configurar-mcps.md

# Outputs de renderização e arquivos pesados (binários)
*.png
*.mp4
*.mov
*.zip
*.pdf
*.tif
*.tiff
*.psd
*.ai
*.pptx
*.zip
# Exceção: assets do tema Shopify precisam ser versionados
!clientes/*/shopify-theme/assets/*.png

# Arquivos de dados do usuário (CSVs, planilhas, etc)
dados/*.csv
dados/*.xlsx
dados/*.xls
dados/*.txt
dados/*.pdf
dados/*.json
!dados/README.md

# Pastas de trabalho do usuário (geradas pelo /setup, conteúdo pessoal)
# clientes/
# propostas/
# conteudo/
# projetos/
# briefings/
trabalho/
estudos/
publicacoes/
anotacoes/
curriculo/
# tarefas.md

# Vida pessoal da Letícia (roda da vida, saúde, emocional) — fica SÓ local, nunca vai pro GitHub
"Letícia Particular/vida/"

# Widgets Scriptable (contêm tokens de API)
"Letícia Particular/scriptable/"
letícia/scriptable/
*/scriptable/*.js

# Dados de cliente com tokens hardcoded
clientes/smartbase/dados/

# Mídia bruta de conteúdo (fotos/vídeos da Letícia) — fica só local, não vai pro GitHub
clientes/smartbase/conteudo/fotos-caxias/
clientes/smartbase/marca/banco de imagens/

# Marcador transitorio da trava de revisao (trava 1), nao versionar
.claude/.pending-review

# Sistema
.DS_Store
node_modules/

# venv de ferramentas de imagem (pesado, nao versionar)
**/_ferramentas/.venv-rembg/
**/.venv-rembg/
# sources brutos de imagem (pesados) — manter so os webp de producao
clientes/smartbase/fotos oficias/**/*.jpg
clientes/smartbase/fotos oficias/**/*.jpeg
clientes/smartbase/fotos oficias/**/*.png
