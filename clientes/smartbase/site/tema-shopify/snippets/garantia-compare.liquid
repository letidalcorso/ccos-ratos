{%- comment -%} Painel comparativo de garantia (novos vs seminovos). Portado de FaqPage.jsx (GarantiaCompare). {%- endcomment -%}
<div class="gar-panel">
  <div class="gar-col">
    <span class="eyebrow">novos</span>
    <div class="gar-t">{{ novos_title }}</div>
    <p>{{ novos_text }}</p>
  </div>
  <div class="gar-col">
    <span class="eyebrow">seminovos</span>
    <div class="gar-t">{{ semi_title }}</div>
    <p>{{ semi_text }}</p>
  </div>
</div>
