# Status — Site Smartbase (tema Shopify)

> Atualizado em 2026-06-13. Esta é a fonte da verdade do estado do site.

## Onde está
- **Tema Shopify completo** em `clientes/smartbase/site/tema-shopify/` (recriado do zero a partir do design do claude.ai/design, pasta `site/design_handoff_shopify_port/`).
- **Publicado (LIVE)** na loja `smartbase-2903.myshopify.com`: nome **smartbase**, ID **#149876408413**. É o tema ativo do site. Push de mudanças: `shopify theme push --theme 149876408413 --allow-live` (de dentro de `tema-shopify/`).
- **Domínio próprio LIVE:** `smartbase-2903.myshopify.com` redireciona (301) para **www.smartbasebr.com** — o domínio já está conectado e é o endereço público do site.
- **Loja ainda protegida por senha** (Online Store → Preferences → Password). A página pública mostra só a tela de senha; o site real (e o Pixel) só renderiza por dentro (logada no admin) ou com a senha. Tirar a senha é o passo de "go público" — pré-requisito pra rodar ads/CTWA e pra o Pixel coletar dados reais.

## Mudanças recentes
- **2026-06-13 — Botão flutuante de WhatsApp (sticky):** novo `snippets/wa-float.liquid` renderizado global no `layout/theme.liquid` (aparece em todas as páginas). Verde oficial do WhatsApp (`#25D366`) + ícone, fixo no canto inferior direito (zona do polegar), círculo no mobile e círculo+rótulo no desktop, pulso sutil (respeita `prefers-reduced-motion`). CSS no fim de `assets/theme.css`. Mensagem **pré-pronta por página** (mesma lógica da faixa: home/vitrine/faq/contato/blog) → o clique já conta como **Lead** (handler de wa.me no `global.js`). Liga/desliga + rótulo no painel (Configurações → "Botão flutuante de WhatsApp"); valores cravados no `settings_data.json` (`wa_float_show:true`, `wa_float_label:"fale com a gente"`) pra não depender de default. **Validado** via `shopify theme dev` (preview local): botão renderiza na home/vitrine/contato com a mensagem correta por página, e o Pixel aparece no HTML da home.
- **2026-06-13 — Correção do diagnóstico do Pixel:** o `settings_schema.json` já trazia o ID como *default* desde 12/06 (commit `0ef718b`), então não dá pra afirmar que o Pixel estava 100% desligado antes (o comportamento do Shopify com default não-salvo é ambíguo). O que é certo: agora o ID está **explícito** no `settings_data.json` (não depende de default) e o preview local confirmou o Pixel no HTML. Na prática não mudou coleta nenhuma porque a loja está atrás de senha (sem visitante real).
- **2026-06-13 — Pixel ligado + CTAs de WhatsApp rastreáveis (rev. de trackeamento):** auditoria dos destinos de botão e do rastreamento. **Meta Pixel LIGADO:** `meta_pixel_id` estava vazio no live (Pixel desligado, site não media nada); preenchido com `2054869865373051` (mesmo Dataset ID das conversões offline, `performance/guia-offline-conversions.md`) e push pro live. Agora o tema dispara **PageView** em toda página e **Lead** em todo clique de botão de WhatsApp (lógica já existia em `assets/global.js` + base do Pixel em `layout/theme.liquid`, gated por `settings.meta_pixel_id`). **CTAs de WhatsApp em branco corrigidos:** hero da home, faixa pré-rodapé (`cta-band.liquid`) e estado vazio da vitrine abriam o WhatsApp sem mensagem; agora levam mensagem pré-pronta **com marcador de origem por página** (ex.: home "oi! vim pelo site, quero saber dos aparelhos"; vitrine "...tava vendo a vitrine..."; contato "...vim pela página de contato..."). Reduz fricção e deixa a origem do lead visível na própria conversa (sem CRM). Produtos já levavam "oi! tenho interesse no [modelo]. ainda tem?". Pull do live feito antes (capturou a remoção do "iPad novo" da vitrine, feita pela Letícia no painel → vitrine com 19 produtos). **Falta validar o disparo:** com a loja logada/sem senha, abrir o site com a extensão *Meta Pixel Helper* e confirmar PageView + Lead no clique do WhatsApp.
- **2026-06-12 — Ajustes da Letícia (painel) capturados + mapa real no contato:** a Letícia reescreveu a HOME no painel (hero com foto real `Gold-Gate-wallpaper-dark.jpg` + badge + lead; passos "você nos chama / identificamos o modelo ideal / primeiro chega depois paga"; banda "tem muita revenda Apple / nenhuma entrega a experiência smartbase"; nota "por que os novos são sob encomenda" explicando garantia de fábrica; card "primeiro chega" com imagem `home_-_sobre.png`; Diferenciais movida pro fim, título "encontramos junto / o aparelho ideal para você"; showcase com foto real). Aplicou TODAS as descrições de produto novas e **adicionou acessórios** (cabo USB-C, fonte 20W) → vitrine com 20 produtos. **Tudo capturado pro repo** (`index.json` + `settings_data.json` sincronizados do live; nunca o contrário). **Contato:** mapa decorativo trocado por **mapa real do Google** (estilizado escuro via filtro CSS) + botão **"traçar rota no Waze"** (deep link por coordenadas). Coordenada editável no painel (campo "Coordenadas (lat,long)", default `-29.16877,-51.17984` = Rua Sinimbu, centro). **Importante:** home e páginas internas agora são editadas no PAINEL — sempre puxar (`shopify theme pull`) antes de mexer, pra não sobrescrever.
- **2026-06-12 — Copy v2 (desejo + voz):** reescrita por desejo de produto e menos genérico (proposta em `site/copy-v2-proposta.md`, specs conferidos). HOME (código): **Posicionamento removida** (redundante com o card "só Apple"); Diferenciais agora titula "qual Apple é a tua? / a gente acha junto"; card "primeiro chega" cita imei; showcase "escolhe o teu. / a gente traz." + seminovos "outro dono, mesma Apple." (assinatura dos seminovos, da Letícia). VITRINE (painel/settings_data): 15 descrições do catálogo reescritas com fato concreto real (8x zoom, 5,6mm, M4, batimento no AirPods Pro 3 etc.) — **os 3 seminovos reais (preços/specs) ficaram intactos**. Hero lead = versão da Letícia (faixa de categorias). Backup do estado anterior em `site/backups/settings_data-antes-copy-v2.json`. **Importante:** as páginas internas renderizam do painel (settings_data), não do código — editar lá ou via push de settings_data.
- **2026-06-09 — Revisão de copy (tom de voz) + correções pré-ar:** revisão das 13 seções contra `conteudo/voz-smartbase.md`. Aplicado no tema live: removido todo travessão `—`, o "não é X é Y" da zona clara, "premium" (AirPods Max), "semi novos"→"seminovos". **Geografia alinhada ao briefing** (Caxias/Serra; tirado "Brasil inteiro/todo o Brasil" da home, contato e FAQ — o trunfo "paga quando recebe" é presencial). **Marcas padronizadas** (Apple/iPhone/WhatsApp maiúsculo; cidades minúsculas). Matados placeholders que apareciam no ar (observação da home + "[ rua, número ]" do contato). Auditoria das outras 3 lentes (conversão/UX/SEO) registrada em `site/melhorias-pos-lancamento.md` (backlog do "depois").
- **2026-06-09 — Botão 2 (secundário) na faixa pré-rodapé:** cada página ganhou um 2º botão de vidro (`{p}_btn2_label` / `_btn2_url` / `_btn2_blank`) que **só aparece quando o texto está preenchido** (liga/desliga por página). Padrão: vitrine = "tirar dúvidas no FAQ" → página FAQ; demais vazias. Mantidas as nossas classes `.btn`/`.btn-glass` (não as `.sb-btn` do pacote). Heroes já eram por página (image_picker próprio) — sem mudança. Implementação porta os recursos do `design_handoff_shopify_port/cta-band.liquid` pra nossa faixa global (roteamento por handle), sem migrar pra templates JSON por página.
- **2026-06-08 — Faixa pré-rodapé por página + correção do FAQ:**
  - `sections/cta-band.liquid` reescrita: copy/botão/**destino** (whatsapp · vitrine · link) próprios por página (home, vitrine, faq, contato, blog) + **liga-desliga por página**, tudo no painel (Personalizar → Faixa pré-rodapé), agrupado por página. IDs de campo mudaram (h_/v_/f_/c_/b_) — se houver customização antiga salva, volta aos novos defaults validados.
  - `sections/faq.liquid`: removido o "fechamento" embutido (`.faq-end`) que duplicava o CTA — agora só a faixa unificada (destino = vitrine). Hero do FAQ passou a usar `amb-iphones.png` (antes dividia a foto do blog).
- Código versionado no GitHub (auto-sync do workspace).
- O one-page HTML antigo (`site/index.html` etc.) foi **substituído** por este tema — não usar mais como referência ativa.

## O que está pronto (fiel ao protótipo)
- **Navegação:** BrowserBar (topo, estilo navegador) + Sidebar de ícones (início · vitrine · faq · contato · blog). NÃO tem top-nav de produtos (decisão: seguir o design system).
- **Home** (`templates/index.json`): hero, posicionamento, diferenciais, zona clara (emblema "sb" + banda editorial + como funciona), vitrine embutida (6 categorias), faixa pré-rodapé.
- **Vitrine** (`/pages/vitrine`): hero + 2 portas (garantias) + filtros (condição + categoria, client-side) + 15 aparelhos em cards + "quero esse" → WhatsApp + estado vazio.
- **FAQ** (`/pages/faq`): acordeão por categoria + painel comparativo de garantia + fechamento.
- **Contato** (`/pages/contato`): hero + mapa decorativo + 3 cards (endereço/horário/atendimento).
- **Blog** (`/pages/blog`): grade de 8 posts + filtros por categoria/tag.
- **Global:** rodapé + faixa de WhatsApp em todas as páginas. Conversão 100% WhatsApp (5554996624494), sem carrinho (v1).
- Tudo **editável no painel** (textos, imagens, produtos, perguntas, posts). Produtos e posts são **blocos no tema** (não produtos nativos do Shopify) — decisão da Letícia, encaixa no modelo só-WhatsApp.

## Decisões técnicas importantes
- **Páginas via `templates/page.liquid`** que roteia por handle (vitrine/faq/contato/blog) → usa modelo "Página padrão" no admin, sem precisar publicar o tema pra aparecer modelo custom. As 4 páginas precisam existir no admin com esses handles exatos.
- **Imagens nativas** (`<img>`), não o `<image-slot>` do protótipo (era frágil). Filtro "cool" e fill via `assets/theme.css`.
- **Animações de entrada desligadas** de propósito: `theme.css` força `.rise`/`.stagger`/`[hidden]` visíveis pra não sumir conteúdo (Safari não suporta `animation-timeline`).
- Imagens são **placeholders** (Apple oficiais / referência do handoff), pesadas (~6MB algumas) — otimizar antes de público.

## O que falta
1. **Trocar imagens placeholder** pelas reais (no painel → Personalizar).
2. ~~Publicar o tema~~ — feito (tema está LIVE). Falta revisar a vitrine de produtos com o dono.
3. **Animações de entrada** (CSS scroll-driven, quando quiser).
4. **Painel de ajustes globais (Tweaks)** — opcional, não feito.
5. **Go público de verdade:** ~~domínio próprio~~ (feito — www.smartbasebr.com) + **tirar a senha da loja** (Online Store → Preferences → Password). Pré-requisito pra ads/CTWA e pra o Pixel coletar dados.
6. Otimizar peso das imagens.
7. **Validar o Pixel** (Meta Pixel Helper) assim que a senha sair — confirmar PageView + Lead no clique de WhatsApp.
8. **Backlog de conversão (CRO/SEO):** prova social, WhatsApp flutuante (sticky), faixa de 3 selos acima da dobra, H1/meta/OG. Detalhe e prioridade em `site/melhorias-pos-lancamento.md`.

## Como retomar
1. No Terminal, na pasta `tema-shopify/`: `shopify theme dev --store smartbase-2903.myshopify.com` (preview ao vivo em `http://127.0.0.1:9292`).
2. Para reenviar mudanças pro tema na loja: `shopify theme push` (escolhe o tema smartbase #149876408413).
3. Páginas: vitrine, faq, contato, blog já criadas no admin (handles iguais). Home é `/`.
