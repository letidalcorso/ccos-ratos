<section class="sb-section">
  <div class="sb-container">
    <h1 style="font-family: var(--font-display); font-size: clamp(32px, 5vw, 56px); font-weight: 800; margin-bottom: 32px;">{{ page.title }}</h1>
    <div style="font-size: 16px; font-weight: 300; color: var(--text-secondary); line-height: 1.8; max-width: 720px;">
      {{ page.content }}
    </div>
  </div>
</section>
